<?xml version="1.0"?>
<!-- Test fixture: Sample XML with various text patterns -->
<component>
  <view extends="lv_obj">
    <!-- Static translatable text -->
    <text_body text="Print Files"/>
    <text_heading text="Settings"/>
    <text_small text="Tip:"/>

    <!-- Dynamic text (should be skipped) -->
    <text_body name="status" bind_text="status_text"/>
    <text_heading bind_text="printer_type"/>

    <!-- Icon text (should be skipped - starts with #icon_) -->
    <lv_label text="#icon_question_circle" style_text_font="mdi_icons_24"/>
    <lv_label text="#icon_home"/>

    <!-- Variable references (should be skipped - contains $) -->
    <text_body text="$label"/>
    <text_small text="$description"/>

    <!-- Component props that may have translatable text -->
    <ui_button label="Save" description="Save current settings"/>
    <ui_card title="Temperature" subtitle="Nozzle and bed"/>

    <!-- Empty and numeric (should be skipped) -->
    <text_body text=""/>
    <text_small text="123"/>
    <text_small text="100%"/>

    <!-- Nested structure -->
    <lv_obj>
      <lv_obj>
        <text_body text="Nested Text"/>
      </lv_obj>
    </lv_obj>

    <!-- Special characters that need to work -->
    <text_body text="Save &amp; Exit"/>
    <text_body text="Temperature: 200°C"/>

    <!-- Multiple text in same element type -->
    <text_body text="First"/>
    <text_body text="Second"/>
    <text_body text="First"/>  <!-- Duplicate should only appear once -->
  </view>
</component>
